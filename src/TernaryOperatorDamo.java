public class TernaryOperatorDamo {
  public static void main(String[] args) {
    // 조건식 ? 연산식(true) : 연산식(false)
    boolean res = (1 !=1) ? true : false;
    // 조건식이 연산식 중 하나인지에 따라 출력된다.
    System.out.println(res);
  }
}
